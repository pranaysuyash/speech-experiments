# DeepFilterNet Dependencies
# Install: pip install -r models/deepfilternet/requirements.txt
# NOTE: PyTorch heavyweight, use isolated venv

# DeepFilterNet package
deepfilternet>=0.5.0

# PyTorch (required, install separately based on platform)
# torch>=1.9.0
# torchaudio>=0.9.0

# Audio processing
numpy>=1.20.0
librosa>=0.10.0
soundfile>=0.12.0
