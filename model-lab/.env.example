# Copy to .env and fill values. Do NOT commit .env.

# Hugging Face auth (required for gated models like pyannote)
HF_TOKEN=hf_xxx
HUGGINGFACE_HUB_TOKEN=hf_xxx

# Optional: keep HF cache local to repo (helps sprint reproducibility)
HF_HOME=.huggingface

